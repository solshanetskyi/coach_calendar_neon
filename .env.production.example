# Production Environment Configuration
# DO NOT commit this file with real values!
# Use this as a reference for setting environment variables in AWS

# ===========================================
# Database - Production
# ===========================================
# Get this from your Neon Production project/branch
# IMPORTANT: Use AWS Secrets Manager for production!
DATABASE_URL=postgresql://neondb_owner:npg_Aho5nl0CPtHZ@ep-quiet-darkness-ag9g8x6n-pooler.c-2.eu-central-1.aws.neon.tech/bookings?sslmode=require&channel_binding=require

# ===========================================
# Server Configuration
# ===========================================
PORT=8080

# ===========================================
# Email Configuration - AWS SES (Recommended)
# ===========================================
USE_AWS_SES=true
SMTP_FROM=verified@yourdomain.com
AWS_REGION=us-east-1

# Requirements for AWS SES:
# 1. Verify sender email in AWS SES Console
# 2. Attach IAM role with SES permissions to your compute resource
# 3. Request production access (to send to unverified emails)

# ===========================================
# Alternative: SMTP (if not using AWS SES)
# ===========================================
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_FROM=verified@yourdomain.com
# SMTP_PASSWORD=your-smtp-password

# ===========================================
# Zoom Configuration (Production)
# ===========================================
# ZOOM_ACCOUNT_ID=your-account-id
# ZOOM_CLIENT_ID=your-client-id
# ZOOM_CLIENT_SECRET=your-client-secret

# ===========================================
# AWS Deployment Notes
# ===========================================
# For AWS App Runner: Set environment variables in the console or apprunner.yaml
# For AWS ECS: Set in task definition environment section
# For AWS Lambda: Set in function configuration
# For AWS Elastic Beanstalk: Set in .ebextensions/environment.config

# SECURITY BEST PRACTICE:
# Store DATABASE_URL and other secrets in AWS Secrets Manager
# Reference them in your service configuration instead of hardcoding
